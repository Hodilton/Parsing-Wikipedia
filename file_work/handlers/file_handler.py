from utils.utilities import Raise
class FileHandler:
    def write(self, data_path, data, overwrite):
        Raise.format_file_error()
        return False

    def read(self, data_path):
        Raise.format_file_error()
        return None